%h1 Просмотр корзины

%table
  %thead
    %tr
      %th
      %th
      %th
      
  %tbody
    - @carts.each do |cart|
      %tr
        %td= link_to 'Показать', cart
        %td= link_to 'Изменить', edit_cart_path(cart)
        %td= link_to 'Удалить', cart, :method => :delete, :data => { :confirm => 'Вы уверены?' }

%br

= link_to 'Новая корзины', new_cart_path
